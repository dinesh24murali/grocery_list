import {StyleSheet} from 'react-native';

const form = StyleSheet.create({
  flexAlignCenter: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default form;
